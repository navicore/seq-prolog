% basic.sprolog - Basic Prolog tests

% Simple facts
likes(mary, food).
likes(mary, wine).
likes(john, wine).
likes(john, mary).

% Simple rule
happy(X) :- likes(X, wine).

% Multi-body rule
friend(X, Y) :- likes(X, Z), likes(Y, Z).
